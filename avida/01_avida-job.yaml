apiVersion: batch/v1
kind: Job
metadata:
  name: avida-job
spec:
  completionMode: Indexed
  parallelism: 5
  completions: 5
  template:
    spec:
      containers:
      - name: avida
        image: avida:local
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        command:
        - /bin/bash
        - -exc
        - |
          cd /avida
          ./run-avida.txt
      restartPolicy: Never
  backoffLimit: 4
